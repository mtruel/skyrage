services:
  skyrage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: skyrage-app
    ports:
      - "8000:8000"
    volumes:
      # Mount database directory for persistence
      - ./data:/app/data
    restart: unless-stopped
